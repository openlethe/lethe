{
  "id": "mentholmike-lethe",
  "name": "Lethe",
  "description": "Persistent memory layer for AI agents — the antidote to the river Lethe.",
  "version": "0.3.3",
  "kind": "context-engine",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "endpoint": {
        "type": "string",
        "description": "Lethe server endpoint (e.g. http://localhost:18483)"
      },
      "apiKey": {
        "type": "string",
        "description": "API key for Lethe server authentication (optional)"
      },
      "agentId": {
        "type": "string",
        "description": "Agent identifier used when creating sessions"
      },
      "projectId": {
        "type": "string",
        "description": "Project identifier for grouping sessions"
      },
      "autoLog": {
        "type": "boolean",
        "description": "Enable diagnostic automatic event logs for tool calls/thread markers. Checkpoints are always written. Default: false.",
        "default": false
      }
    }
  }
}
